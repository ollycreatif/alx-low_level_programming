#include "main.h"
/**
 * _strcmp - compares two strings
 * @s1: first string
 * @s2: second string
 * Return: number of common characters
*/
int _strcmp(char *s1, char *s2)
{
}

/**
 * _strchr - locates character in string
 * @s: string to check
 * @c: character to find
 * Return: pointer to the first occurrence of the character
*/
char *_strchr(char *s, char c)
{
}

/**
 * _strspn - calculates the length of a prefix substring
 * @s: string to check
 * @accept: bytes of prefix substring in question
 * Return: accept
*/
unsigned int _strspn(char *s, char *accept)
{
}

/**
 * _strpbrk - searches a string for any set of bytes
 * @s: string to search
 * @accept: bytes of string in question
 * Return: accept
 */
char *_strpbrk(char *s, char *accept)
{
}

/**
 * _strstr - finds  the  first  occurrence  of  a
 * substring in a string
 * @haystack: string to search
 * @needle: substring find
 * Return: pointer to the beginning of the located substring,
 * or  NULL  if  the substring is not found
 */
char *_strstr(char *haystack, char *needle)
{
}
